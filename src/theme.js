import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      light: '#92c0ba',
      main: '#77B1A9',
      dark: '#537b76'
    },
    secondary: {
      light: '#ebcd8b',
      main: '#E6C16F',
      dark: '#a1874d',
    },
    background: {
      default: '#fff',
    },
  },
});

export default theme;
